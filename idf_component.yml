## IDF Component Manager Manifest File
name: "sendspin-cpp"
dependencies:
  idf: ">=5.5"
  espressif/esp_websocket_client: ">=1.0.0"
  bblanchon/arduinojson: ">=7.4.2"
  esphome/micro-flac:
    version: ">=0.1.1"
    rules:
      - if: "$CONFIG{SENDSPIN_ENABLE_PLAYER} == True"
  esphome/micro-opus:
    version: ">=0.3.5"
    rules:
      # SENDSPIN_ENABLE_OPUS only exists while the player is enabled, so test the player first
      - if: "$CONFIG{SENDSPIN_ENABLE_PLAYER} == True && $CONFIG{SENDSPIN_ENABLE_OPUS} == True"
description: "Sendspin synchronized audio streaming client for ESP32"
license: "Apache-2.0"
maintainers:
  - Kevin Ahrendt <kevin.ahrendt@openhomefoundation.org>
version: "0.0.0"
url: https://github.com/sendspin/sendspin-cpp
repository: https://github.com/sendspin/sendspin-cpp.git
files:
  exclude:
    - ".claude/**"
    - ".github/**"
    - ".clang-*"
    - ".clangd"
    - ".markdownlint.yaml"
    - ".pre-commit-config.yaml"
    - "examples/**"
    - "test/**"
    - "script/**"
    - "CLAUDE.md"
